[package]
name = "nilm-core"
version = "0.1.0"
edition = "2021"
description = "Rust/WASM core for NILM: metrics, resampling, combinatorial optimization"
license = "MIT"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
wasm-bindgen = "0.2"

[profile.release]
opt-level = 3
lto = true
